<nav class="px-2 bg-white shadow dark:bg-gray-800 rounded">
    <div class="px-6 py-3 mx-auto md:flex md:justify-between md:items-center">
        <div class="flex items-center justify-between">
            <div>
                <a href="#" class="text-xl font-bold text-blue-500 dark:text-white md:text-2xl hover:text-blue-700 dark:hover:text-gray-300">
                    Borne Manager
                </a>
            </div>

            <!-- Mobile menu button -->
            <div class="flex md:hidden">
                <button type="button" class="text-gray-500 dark:text-gray-200 hover:text-gray-600 dark:hover:text-gray-400 focus:outline-none focus:text-gray-600 dark:focus:text-gray-400  " aria-label="toggle menu">
                    <svg viewBox="0 0 24 24" class="w-6 h-6 fill-current">
                        <path fill-rule="evenodd" d="M4 5h16a1 1 0 0 1 0 2H4a1 1 0 1 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2z"></path>
                    </svg>
                </button>
            </div>
        </div>

        <!-- Mobile Menu open: "block", Menu closed: "hidden" -->
        <div class="items-center md:flex">
            <div class="flex flex-col md:flex-row md:mx-6">
               {% if is_granted("ROLE_ADMIN") %}
                    <a href="{{ path('lang_list') }}" 
                        class="my-1 text-gray-700 dark:text-gray-200 hover:text-blue-500 dark:hover:text-blue-400 md:mx-4 md:my-0">
                        Lang
                    </a>
               {% endif %}
                <a href="{{ path('catalog_lang', {'idLang': 1}) }}" 
                    class="my-1 text-gray-700 dark:text-gray-200 hover:text-blue-500 dark:hover:text-blue-400 md:mx-4 md:my-0">
                    Catalogue
                </a>
                {% if is_granted("ROLE_ADMIN") %}
                <a href="{{ path('event') }}" 
                    class="my-1 text-gray-700 dark:text-gray-200 hover:text-blue-500 dark:hover:text-blue-400 md:mx-4 md:my-0">
                    Ajouter
                </a>
                {% endif %}
            </div>

            <div class="flex justify-center md:block">
                {% if app.user %}
                     <a href="{{ path('app_logout') }}" 
                    class="my-1 text-gray-700 dark:text-gray-200 hover:text-blue-500 dark:hover:text-blue-400 md:mx-4 md:my-0">
                        Déconnexion
                    <span class=" absolute top-0 left-0 p-1 text-xs text-white bg-blue-500 rounded-full "></span>
                    </a>
                {% else %}
                      <a href="{{ path('app_logout') }}" 
                        class="my-1 text-gray-700 dark:text-gray-200 hover:text-blue-500 dark:hover:text-blue-400 md:mx-4 md:my-0">
                            Se connecter
                        <span class=" absolute top-0 left-0 p-1 text-xs text-white bg-blue-500 rounded-full "></span>
                    </a>
                {% endif %}

            </div>
        </div>
    </div>
</nav>
